import type { BaseUtile } from '@/utils/BaseUtile/BaseUtile';

export interface EmptyPageProps {
  title?: string;
  subtitle?: string;
  actionOn?: boolean;
  actionText?: string;
  actionUtile?: BaseUtile;
}
